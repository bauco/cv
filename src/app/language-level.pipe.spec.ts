import { LanguageLevelPipe } from './language-level.pipe';

describe('LanguageLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
